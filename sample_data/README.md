# sample_data/ — ダミーデータ置き場（Git 管理対象）

**個人情報を含まない**架空のデータのみを置いてください。
このフォルダはコミットされ、GitHub に公開されます。

開発・動作確認はここのデータで行います。実データと同じ列名・型・件数感を
再現しておけば、中身が架空でもコードは書けます。

実データは `data/` へ（Git 管理外・Claude 読み取り不可）。
`data/` はクローン後に自分で作成してください（`mkdir data`）。
