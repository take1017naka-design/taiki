# taiki

## このリポジトリの取り扱い方針（最重要）

このプロジェクトは医療現場で使われる可能性があります。**患者個人情報（PHI）を
リポジトリにもAIの文脈にも一切載せない**ことを最優先のルールとします。

### 絶対にやらないこと

- 患者氏名・カルテID・生年月日・住所・電話番号・保険証番号を、コード・コメント・
  コミットメッセージ・Issue・PR のいずれにも書かない
- 電子カルテや部門システムから書き出した実データを `git add` しない
- 実データを Claude に読ませない（貼り付けもしない）

### データの置き場所

| フォルダ | 用途 | Git | Claude の読み書き |
|---|---|---|---|
| `data/` | 実データ（個人情報を含みうる） | 追跡しない | `.claude/settings.json` で拒否 |
| `sample_data/` | ダミー・匿名化済みデータ | 追跡する | 可 |

`data/` はリポジトリに含まれません。クローン後に `mkdir data` で作ってください。
Claude はこのフォルダを読むことも書くこともできません（この制約は実際に検証済みです）。

動作確認やコード作成は、必ず `sample_data/` のダミーデータで行ってください。
構造（列名・型）さえ同じなら、中身が架空でも開発には十分です。

### 匿名化の目安

実データを使わざるを得ない場合も、Claude に渡す前に以下を落としてください。

- 直接識別子: 氏名、カルテID、住所、電話番号、メールアドレス、顔写真
- 準識別子: 生年月日（→ 年齢や年代へ）、正確な受診日（→ 相対日数へ）、
  希少疾患名と施設名の組み合わせ

### AI に渡る範囲についての注意

Claude Code はローカルで動きますが、**あなたが読ませたファイルの中身は
Anthropic の API に送信されます**。「ローカルで動く = 何も外に出ない」では
ありません。外に出さない唯一の確実な方法は、そのファイルを Claude に触らせない
ことです。上の `deny` 設定と `.gitignore` はそのための仕組みです。

## 開発

ローカル（Windows）でのセットアップ手順は [`docs/local-setup.md`](docs/local-setup.md) を参照。

## コミット前チェック

`.githooks/pre-commit` が、個人情報を含みやすいファイルの誤コミットを止めます。
クローン後に一度だけ有効化してください。

```bash
git config core.hooksPath .githooks
```
